rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Deny all direct client access — files are served via backend API only.
      // TODO: Add authenticated read access once Firebase Auth is wired up.
      allow read, write: if false;
    }
  }
}
